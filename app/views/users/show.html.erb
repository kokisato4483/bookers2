
 <div class="box">
<div class="container">
<div class="row">

 <div class= "col-xs-3">
   
 <%= render 'books/book', user: @user %>
  

  <div class= "col-xs-8">
    <h2>Books</h2>
      <table class='table table-hover table-inverse'>
      <tr>
        <th></th>
        <th>Title</th>
        <th>Opinion</th>
        <th colspan="3"></th>
      </tr>
<% @books.each do |book| %>

        <tr>
          <td>
            <%= link_to user_path(@user.id) do %>
            <nav><%= image_tag @user.get_profile_image(100,100) %></nav>
          </td> 
        <% end %>
          <td><%=link_to book.title, book_path(book.id) %></td>
          <td><%= book.body %></td>
        </tr>
<% end %> 
  </table>
  </div>  <!-- この行を追加 -->
</div>    <!-- この行を追加 -->
</div>
</div>
   


